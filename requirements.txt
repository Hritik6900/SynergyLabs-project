# --- Core RAG stack ---
chromadb==0.5.23            # low-cost, embedded, persistent local vector store
fastapi==0.115.6           # HTTP API
uvicorn[standard]==0.34.0  # ASGI server
httpx==0.28.1              # test client + HTTP calls
pydantic==2.10.4           # request/response models + settings
pydantic-settings==2.7.1   # env-var driven config
python-dotenv==1.0.1       # load .env in local dev

# --- Embeddings + generation (providers are optional at runtime) ---
openai==1.59.6             # text-embedding-3-small + gpt-4o-mini
anthropic==0.42.0          # claude-haiku generation (alternative LLM)
tiktoken==0.8.0            # token-accurate chunking

# --- Document loading ---
pypdf==5.1.0               # PDF text extraction
beautifulsoup4==4.12.3     # HTML -> text
lxml==5.3.0                # fast HTML parser backend for bs4

# --- Evaluation ---
numpy==2.2.1               # IR metric math (nDCG, etc.)
